$('#datagrid').html('<%= escape_javascript(
  
       render(:layout=>"datagrid") { |user| 
         link_to( t(:show), user)+" "+link_to(t(:edit), edit_user_path(user))+" "+link_to(t(:destroy), user, confirm: t(:confirm), method: :delete );
       }

) %>');  