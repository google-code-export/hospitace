$('#datagrid').html('<%= escape_javascript(
  
       render(:layout=>"datagrid") { |observation| 
         link_to( 'Show', observation)+" "+link_to( 'Edit', edit_observation_path(observation))+" "+link_to('Destroy', observation, confirm: 'Are you sure?', method: :delete );
       }

) %>');  