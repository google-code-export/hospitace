<% title "New Order" %>

<% form_for @test do |f| %>
  <%= render "#{@test.current_step}_step", :f => f %>
  <p><%= f.submit "Continue" %></p>
  <p><%= f.submit "Back", :name => "back_button" unless @test.first_step? %></p>
<% end %>


<p><%= link_to "Back to List", tests_path %></p>

