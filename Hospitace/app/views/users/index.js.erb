$('#datagrid').html('<%= escape_javascript(
  
       render(:layout=>"datagrid") { |user| 
         link_to( 'Show', user)+" "+link_to( 'Edit', edit_user_path(user))+" "+link_to('Destroy', user, confirm: 'Are you sure?', method: :delete );
       }

) %>');  