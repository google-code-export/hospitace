<% title :evaluation, :small=>"z výuky #{@observation.course.name} (#{@observation.course.code})" %>

<% unless Form.find_by_form_template_id(2,@evaluation.id).nil? %>
  <%= draw_dynamic_form(2,@evaluation.id) %>
<% else %>
  <%= dynamic_form(2,@evaluation.id,  :html => { :class => 'form-vertical' }) %>
<% end %>
