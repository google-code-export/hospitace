<p>
<%= render(note.user) %> | <%= l(note.updated_at, :format=>:long) %> 
| <%= link_to t(:edit), edit_observation_note_path(@observation,note) %>
| <%= link_to t(:destroy), note ,method: :delete %>

</p>
<p>
  <%= note.note.html_safe %>  
</p>

<hr />
