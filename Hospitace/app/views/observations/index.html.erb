<% title :observations, :small=>"naplánované hospitace v semestru "+select( "selected_semester", "code", Semester.find_used_current_and_next.collect {|p| [p.name, p.code]}, :selected=>semester.code).html_safe, :action_title=>'+ Vytvořit hospitaci',:action=>new_observation_path %>

<%#= select( "selected_semester", "code", Semester.find_current_and_next.collect {|p| [p.name, p.code]}, :selected=>semester.code) %>

<%= render :layout=>'datagrid' do |observation| %>

    <%= link_to 'Show', observation %>
    <%= link_to 'Edit', edit_observation_path(observation) %>
    <%= link_to 'Destroy', observation, confirm: 'Are you sure?', method: :delete %> 

<%end%>
<br />

