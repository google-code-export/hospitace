<%= form_for(@opinion) do |f| %>
  <% if @opinion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@opinion.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @opinion.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

 <%= f.hidden_field :evaluation_id,{"value"=> params[:evaluation_id]} %>

  <div class="field">
    <%= f.label :opinion, "Stanovisko:" %><br />
    <%= f.text_area :opinion, {"class" => "grid_16", "rows"=> "10"} %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>