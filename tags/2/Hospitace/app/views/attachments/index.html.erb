<% title :attachment %>

<table class="table table-striped">
  <thead>
  <tr>
    <th>Název souboru</th>
    <th>Typ</th>
    <th>Autor</th>
    <th></th>
    <th></th>
  </tr>
  </thead>
  <tbody>
  <% @attachments.each do |attachment| %>
    <tr>
      <td><%= attachment.filename %></td>
      <td><%= attachment.content_type %></td>
      <td><%= attachment.user.full_name %></td>
      <td><%=
            actions do 
              action_icon( :show,"icon-info-sign",attachment,{},:show,attachment) + " "+
              action_icon( :destroy,"icon-remove",attachment,{confirm: t(:confirm), method: :delete},:destroy,attachment)
            end
          %>
      </td>
    </tr>
  <% end %>
  </tbody>
</table>

<%= render 'form'  %>