$('#datagrid').html('<%= escape_javascript(
  
       render(:layout=>"datagrid") { |observation| 
         link_to_if(can?(:show, observation),t(:show), observation)+" "+link_to_if(can?(:destroy, observation),t(:destroy), observation, confirm:  t(:confirm), method: :delete );
       }

) %>');  